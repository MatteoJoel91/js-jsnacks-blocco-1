// Stampa il cubo dei primi N numeri, dove N è un numero indicato dall’utente.

// numero = parseInt(prompt('Inserire un numero'));
// cubo = Math.pow(numero, numero);
// console.log(cubo);

let numero = parseInt(prompt('Inserire un numero'));
let cubo = 0;
i=0;
while (i <= numero) {
    cubo = Math.pow(numero,numero)
    console.log('Il cubo di ' + i + ' è ' + );
    i++;
    
}

